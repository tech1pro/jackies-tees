# Jackie's Tees Server Environment
# Copy to .env and adjust as needed.

PORT=5000
NODE_ENV=development
DATABASE_URL=
# Set to true for hosted Postgres providers that require SSL (Supabase/Render/Neon)
DATABASE_SSL=true
ADMIN_TOKEN=change-me-to-a-long-random-value
