# Canonical site URL for Open Graph / canonical tags (no trailing slash).
# Local dev: leave unset — runtime uses window.location.origin.
# Render: usually omit — client/vite.config.js bakes RENDER_EXTERNAL_URL at build time.
# Set only if you need an explicit URL (e.g. custom domain nuances).
VITE_SITE_URL=
